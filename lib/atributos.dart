class Atributos {
  //Atributo
  // int valor = 1;
  // print(valor);
  // double total = 1.35;
  // print(total);
  // String label = 'Total: ';
  // print(label);
  // DateTime agora = DateTime.now();
  // print(agora);
  // dynamic dinamico = total + valor;
  // print(dinamico);
  // var variavel = agora;

  // Object objeto = {'nome': 'Tadeu'};
  // var lista = <String>['Tadeu', 'Lucas'];
  // print(lista);
  // lista.removeAt(0);
  // print(lista);
}
